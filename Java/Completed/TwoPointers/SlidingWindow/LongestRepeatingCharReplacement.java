package Completed.TwoPointers.SlidingWindow;

public class LongestRepeatingCharReplacement {
    public int characterReplacement(String s, int k) {
        //Two pointers:
        //  slow, fast which is ahead by one
        //slow keeps track of the front of the substring
        //fast moves if the next char matches the first pointer's char and increment the length
        //otherwise, if another char exists, substract k if above 0 and add to length
        return 0;
    }
}
